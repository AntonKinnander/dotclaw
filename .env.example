# DotClaw environment variables
#
# IMPORTANT: This file should be placed at ~/.dotclaw/.env
# (or $DOTCLAW_HOME/.env if you've customized the home directory)
#
# Note: Only secrets belong in .env for the main app runtime.
# Some variables below are for one-off scripts and must be set in your shell
# (they are NOT read from .env unless the script explicitly loads dotenv).

# --- Required (app runtime) ---
# Set at least one provider token (Telegram or Discord).

# Telegram bot token from @BotFather
# TELEGRAM_BOT_TOKEN=123456789:replace-with-real-token

# Discord bot token (from Discord Developer Portal)
# DISCORD_BOT_TOKEN=replace-with-discord-token

# Discord bot owner ID (bot will respond to any message from this user)
# DISCORD_OWNER_ID=622332026141409281

# Discord channels to exclude (comma-separated channel IDs)
# DISCORD_EXCLUDED_CHANNELS=1234567890,0987654321

# Discord channel configuration
# Format: channelId|channelName|type|description|defaultSkill
# Types: text, voice, forum
# Skill: optional skill name to auto-load for this channel
#
# Format: All entries on one line, separated by double-pipe (||)
# Each entry: channelId|channelName|type|description|defaultSkill
#
# DISCORD_CHANNELS=1424830890298707968|general-chat|text|General chat for any topic||1424831204108013628|planning-room|text|Daily planning workspace - organize tasks, set priorities, plan the day ahead||1424831609869434984|nightly-recap|text|Evening reflection and journaling - user initiates daily recap session||1460262303818584239|to-do|forum|Task tracking forum - create posts for task lists, track progress with replies||1423245269402914868|ideas|forum|Idea board - capture new ideas with titles/tags||1423779555855962243|to-ponder|forum|Topics to explore and solutions needed||1423578526744842331|journal|forum|Daily journal entries - AI posts summary after nightly recap with stats like tasks completed||
#
# Notes:
# - All entries must be on a single line, separated by ||
# - Each entry has 5 pipe-delimited fields: channelId|channelName|type|description|defaultSkill
# - The defaultSkill field is optional (empty between the last two pipes)
# - For forum channels: AI only responds to thread replies, not new posts
# - Voice channels are deferred until voice transcription is implemented

# OpenRouter API key
OPENROUTER_API_KEY=sk-or-replace-with-real-key

# --- Optional (app runtime) ---
# Brave Search API key (enables WebSearch tool in the agent)
BRAVE_SEARCH_API_KEY=replace-with-brave-key

# Override host timezone (defaults to system TZ)
TZ=America/New_York

# GitHub Personal Access Token (enables gh CLI in containers)
# GH_TOKEN=ghp_your_token_here

# Container-only overrides (for example OPENAI_API_KEY or DOTCLAW_VISION_MODEL)
# should be set per group in ~/.dotclaw/data/registered_groups.json under containerConfig.env.

# --- Optional: System (set in shell before starting) ---
# Override DotClaw home directory (defaults to ~/.dotclaw)
# DOTCLAW_HOME=~/.dotclaw

# --- Optional: bootstrap/configure (set in shell when running scripts) ---
# Non-interactive bootstrap/configure flags
# DOTCLAW_BOOTSTRAP_NONINTERACTIVE=1
# DOTCLAW_CONFIGURE_NONINTERACTIVE=1

# Required for non-interactive bootstrap
# DOTCLAW_BOOTSTRAP_CHAT_ID=123456789

# Explicit provider selection (telegram or discord)
# DOTCLAW_BOOTSTRAP_PROVIDER=telegram

# Optional bootstrap defaults
# DOTCLAW_BOOTSTRAP_GROUP_NAME=main
# DOTCLAW_BOOTSTRAP_GROUP_FOLDER=main
# DOTCLAW_BOOTSTRAP_BUILD=true
# DOTCLAW_BOOTSTRAP_SELF_CHECK=true

# Required for non-interactive configure
# DOTCLAW_CONFIGURE_CHAT_ID=123456789

# --- Optional: autotune (advanced) ---
# Autotune uses dotenv, so these can live in .env if you run `npm run autotune`.
# AUTOTUNE_TRACE_DIR=~/.dotclaw/traces
# AUTOTUNE_OUTPUT_DIR=~/.dotclaw/prompts
# AUTOTUNE_CANARY_FRACTION=0.1
# AUTOTUNE_BEHAVIOR_CONFIG_PATH=~/.dotclaw/config/behavior.json
# AUTOTUNE_BEHAVIOR_REPORT_DIR=~/.dotclaw/data
# AUTOTUNE_BEHAVIOR_ENABLED=1

# --- Internal (do not set) ---
# Used by container entrypoint logic
# DOTCLAW_DAEMON=1
# DOTCLAW_SELF_CHECK=1
